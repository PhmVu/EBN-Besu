# Backend Environment Configuration

# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=besu_training
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Authentication
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Blockchain RPC
RPC_URL=http://localhost:8549
RPC_WS_URL=ws://localhost:8550
CHAIN_ID=1337

# Admin Account (for deploying contracts)
# Generate new keys and update these values
ADMIN_ADDRESS=0x...
ADMIN_PRIVATE_KEY=0x...

# Optional: Pre-deployed Contract Addresses
# If contracts are deployed separately, set these to reuse them
# CLASS_MANAGER_ADDRESS=0x...
# SCORE_MANAGER_ADDRESS=0x...

# Logging
LOG_LEVEL=info

# CORS
CORS_ORIGIN=http://localhost:5173

# Wallet Encryption (for student private keys)
WALLET_ENCRYPTION_KEY=your-encryption-key-change-this
